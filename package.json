{
  "name": "travelbalkan",
  "version": "1.0.0",
  "private": true,
  "description": "Editable Balkan trip checklist with live sync (Cloudflare Pages + KV).",
  "scripts": {
    "setup": "bash scripts/setup-cloudflare.sh",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
